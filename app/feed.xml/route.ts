import {NextResponse} from 'next/server'; import {articles} from '@/lib/data';
export async function GET(){const b=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'; const items=articles.map(a=>`<item><title><![CDATA[${a.title}]]></title><link>${b}/articulo/${a.slug}</link><description><![CDATA[${a.excerpt}]]></description><pubDate>${new Date(a.date).toUTCString()}</pubDate></item>`).join(''); const xml=`<?xml version="1.0"?><rss version="2.0"><channel><title>CurioWorld</title><link>${b}</link><description>Descubre algo nuevo cada día.</description>${items}</channel></rss>`; return new NextResponse(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}});}
